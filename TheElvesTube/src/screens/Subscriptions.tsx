import {View, Text} from 'react-native';
import React from 'react';

type Props = {};

const Subscriptions = (props: Props) => {
  return (
    <View>
      <Text className="  font-sans text-lg">Subscription Screen</Text>
    </View>
  );
};

export default Subscriptions;
